import styled from "styled-components";
import { CategoryItem } from "../atoms/CategoryItem";

export const CategoryItemList = () => {
  const categories = ["Minecraft", "画像処理", "モバイルアプリ", "その他"];

  return <CategoryItem text={"テスト"} />;
};
